{
  "name": "fleet-vitals-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && npx serverless deploy",
    "remove": "npx serverless remove",
    "dev": "npm run build && npx serverless offline"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "serverless": "^4.0.0",
    "serverless-offline": "^14.4.0",
    "typescript": "^5.3.3"
  }
}
